# Регресс-чеклист app1.py (после разбиения на src/)

Цель: убедиться, что перенос логики из монолитного `app1.py` в пакет `src/`
(Фазы 1-4) не сломал ни один из пользовательских сценариев. Заполняется
вручную во время реального прогона `streamlit run app1.py
--server.enableXsrfProtection=false` на тестовом (или реальном) наборе
`rec.txt` + папка изображений.

Статус: **не выполнено** — требует ручного прохода мейнтейнером (см.
`.claude/PRPs/plans/completed/ocr-consensus-spike-phases-5-9.plan.md`, Phase 5).
Отметки ✅/❌ и заметки ниже — заполнить по факту прогона.

## Загрузка и рабочая директория

| # | Проверка | Кнопка/действие | Результат |
|---|---|---|---|
| 1 | Загрузка `rec.txt` через file_uploader | `st.file_uploader` | ⬜ |
| 2 | Указание рабочей директории и валидация пути | текстовое поле "Укажите рабочую директорию" | ⬜ |
| 3 | Инициализация `AnnotationManager` и парсинг `rec.txt` | — | ⬜ |

## Список изображений и фильтры

| # | Проверка | Кнопка/действие | Результат |
|---|---|---|---|
| 4 | Фильтр "Все" | радио-кнопка/селект списка | ⬜ |
| 5 | Фильтр "Неразмеченные" | — | ⬜ |
| 6 | Фильтр "Размеченные" | — | ⬜ |
| 7 | Пагинация вперёд | "▶" | ⬜ |
| 8 | Пагинация назад | "◀" | ⬜ |
| 9 | Клик по строке списка переключает `current_idx` | клик по строке | ⬜ |

## Редактор — текст и подтверждение

| # | Проверка | Кнопка/действие | Результат |
|---|---|---|---|
| 10 | Редактирование текста аннотации | textarea | ⬜ |
| 11 | Подтверждение записи: `current_idx` продвигается, `unsaved_changes` растёт | "✓ Подтвердить" | ⬜ |
| 12 | Автосохранение срабатывает при накоплении 10 изменений | — | ⬜ |

## Рукописный текст

| # | Проверка | Кнопка/действие | Результат |
|---|---|---|---|
| 13 | Запись помечается как рукописная, копируется в `handwritten_images/`, строка добавляется в `handwritten.txt` | "✍ Рукописный" | ⬜ |
| 14 | Повторная отметка той же записи не создаёт дубликат строки в `handwritten.txt` | повтор действия 13 | ⬜ |

## Удаление

| # | Проверка | Кнопка/действие | Результат |
|---|---|---|---|
| 15 | Открытие модалки подтверждения удаления | "🗑️" | ⬜ |
| 16 | Подтверждение удаляет файл, запись и создаёт бэкап | "✓ Да" | ⬜ |
| 17 | Отмена ничего не удаляет | "✗ Отмена" | ⬜ |

## Поворот изображения — критичная точка регрессии

> Специально выделено: `src/image_ops.py:23` документирует хрупкую связь
> между `rotate_image` и `load_and_resize_image.clear()`, которая работает
> только пока обе функции находятся в одном модуле. После разбиения на
> `src/` это нужно перепроверить явно, глядя на сам отрендеренный
> превью-снимок, а не только на факт записи на диск.

| # | Проверка | Кнопка/действие | Результат |
|---|---|---|---|
| 18 | Поворот "влево" сохраняет повёрнутое изображение на диск | "↶" | ⬜ |
| 19 | После поворота отображаемая миниатюра меняется **немедленно**, без ручного обновления страницы (проверка сброса `st.cache_data` через `load_and_resize_image.clear()`) | смотреть на превью сразу после клика | ⬜ |
| 20 | Поворот "вправо" аналогично работает и обновляет превью | "↷" | ⬜ |

## Навигация — критичная точка регрессии

> Специально выделено: `src/hotkeys.py:1` документирует хрупкую связь между
> JS-обработчиком горячих клавиш и буквальным текстом кнопок `←`/`→` в
> `src/ui/editor_view.py`. После разбиения на модули нужно перепроверить,
> что оба пути навигации (клик мышью и физическая клавиша) всё ещё работают.

| # | Проверка | Кнопка/действие | Результат |
|---|---|---|---|
| 21 | Переход к предыдущему изображению кнопкой | "←" | ⬜ |
| 22 | Переход к следующему изображению кнопкой | "→" | ⬜ |
| 23 | Переход к предыдущему изображению физической клавишей стрелка-влево | клавиша ← на клавиатуре | ⬜ |
| 24 | Переход к следующему изображению физической клавишей стрелка-вправо | клавиша → на клавиатуре | ⬜ |

## Сайдбар — статистика

| # | Проверка | Кнопка/действие | Результат |
|---|---|---|---|
| 25 | Счётчик "Всего" соответствует числу записей | — | ⬜ |
| 26 | "Размечено" и процент считаются верно | — | ⬜ |
| 27 | "Осталось" = Всего - Размечено | — | ⬜ |

## Сайдбар — сохранение

| # | Проверка | Кнопка/действие | Результат |
|---|---|---|---|
| 28 | Полное сохранение всех изменений, сброс `unsaved_changes` | "💾 Сохранить всё" | ⬜ |

## Сайдбар — бэкапы

| # | Проверка | Кнопка/действие | Результат |
|---|---|---|---|
| 29 | Переключение видимости списка бэкапов | "📋" | ⬜ |
| 30 | Отображаются последние 3 бэкапа с временем и типом операции | — | ⬜ |
| 31 | Восстановление из бэкапа перезаписывает `rec.txt` | "↩️" | ⬜ |

## Итог

- Все пункты ✅ → Фаза 5 завершена, статус PRD можно переводить в `complete`.
- Любой ❌ → завести точечный фикс в соответствующем модуле `src/`, не
  переделывая рефакторинг целиком, и повторно проверить только этот пункт.

### Найденные баги и фиксы

_(заполняется по ходу прогона)_

- Нет данных — чеклист ещё не выполнялся.
