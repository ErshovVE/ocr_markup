[tool.ruff]
target-version = "py312"
line-length = 100
src = ["frontend", "backend"]
exclude = ["legacy", ".venv*", "predict.py", "predict.ipynb"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B"]

[tool.pytest.ini_options]
testpaths = ["frontend/tests", "backend/tests"]
